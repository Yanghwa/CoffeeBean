@model IEnumerable<FinalProject.Models.CoffeeBean>

@{
    ViewBag.Title = "Index";
}
<header class="intro-header" style="background-image: url('../img/post-sample-image.jpg')">
    <div class="container">
        <div class="row">
            <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
                <div class="site-heading">
                    <h1>For Sale</h1>
                    <hr class="small">
                    <span class="subheading">Our favorite coffee</span>
                </div>
            </div>
        </div>
    </div>
</header>
<div class="container">
    <div class="row">
        <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
            <h2>Coffee Beans</h2>

            <p>
                @if (Request.IsAuthenticated)
                {
                    @Html.ActionLink("Create New", "Create")
                }
            </p>
            <table class="table">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Bean.Name)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Coffee.Name)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.CreateDate)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.EditDate)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Bean.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Coffee.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CreateDate)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.EditDate)
                        </td>
                        <td>
                            @Html.ActionLink("Details", "Details", new { id = item.BeanId })
                            @if (Request.IsAuthenticated)
                            {
                                @Html.Raw(" | ")
                                @Html.ActionLink("Edit", "Edit", new { id = item.BeanId })
                                @Html.Raw(" | ")
                                @Html.ActionLink("Delete", "Delete", new { id = item.BeanId })
                            }
                        </td>
                    </tr>
                }

            </table>
        </div>
    </div>
</div>